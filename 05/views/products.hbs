<h1>Lista de Productos:</h1>

    {{!-- {{#if showProds}}
    <ul>
        {{#each products}}
            <li>{{this.name}} {{this.price}}</li>
        {{/each}}
    </ul>
    {{/if}} --}}

    <ul>
        {{#each dataProds}}
            <li>{{this}}</li>
            <li>{{this.title}} {{this.price}}</li>
        {{/each}}
    </ul>